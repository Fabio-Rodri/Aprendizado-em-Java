const passo = 2;
let contador = 1;
++contador; // Operador de Incremento (não pode ser usado em uma constante, apenas let)
--contador; // Operador de Decremento
contador = contador + passo;
contador **= 10 // Operador de Atribuição 
contador += passo; // <- contador = contador + passo;
console.log(contador)
// parseInt (valor inteiro )= pedir para alguma ação ser executada
// parseFloat = numero real
